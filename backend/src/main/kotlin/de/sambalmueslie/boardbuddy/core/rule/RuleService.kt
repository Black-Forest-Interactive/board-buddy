package de.sambalmueslie.boardbuddy.core.rule

import jakarta.inject.Singleton
import org.slf4j.LoggerFactory

@Singleton
class RuleService {
    companion object {
        private val logger = LoggerFactory.getLogger(RuleService::class.java)
    }
}